import { ArrowLeft, ArrowRight, BookOpen, CheckCircle2, Lightbulb, Sparkles } from 'lucide-react';
import { useDemo } from '../DemoContext';
import { Reveal } from '@/components/ui/Reveal';

export function NuruView() {
  const { nuruResponse, nuruQuestion, setStage, clearNuru } = useDemo();
  return <div><Reveal><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-eyebrow">Nuru Assistance</p><h2 className="text-heading mt-2 text-3xl font-bold sm:text-4xl">Help, right where learning happens.</h2><p className="text-body mt-2 max-w-2xl">Nuru responds using the student's current learning context. Choose a question from the floating assistant to see how it guides understanding.</p></div><span className="inline-flex w-fit items-center gap-2 rounded-full border border-brand-200 bg-brand-50 px-3 py-1.5 text-xs font-semibold text-brand-700"><Sparkles className="h-3.5 w-3.5" />Context-aware support</span></div></Reveal><div className="mt-7 grid gap-5 xl:grid-cols-[minmax(0,1fr)_18rem]"><Reveal className="min-w-0"><div className="surface p-5 sm:p-7">{nuruResponse ? <div className="animate-scale-in"><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-600 text-white"><Sparkles className="h-5 w-5" /></span><div><p className="text-xs font-bold uppercase tracking-wider text-brand-600">Nuru's guidance</p><h3 className="text-heading mt-1 text-xl font-bold">{nuruResponse.title}</h3></div></div><div className="mt-6 rounded-xl border border-brand-200 bg-brand-50/60 p-5"><p className="text-base leading-relaxed text-ink-800">{nuruResponse.body}</p>{nuruResponse.example && <div className="mt-4 rounded-lg bg-white px-4 py-3 text-center font-display text-lg font-bold text-ink-900">{nuruResponse.example}</div>}<div className="mt-4 flex items-start gap-2 border-t border-brand-200 pt-4 text-sm text-brand-800"><Lightbulb className="mt-0.5 h-4 w-4 shrink-0" /><span>{nuruResponse.followUp}</span></div></div><div className="mt-6 flex flex-wrap gap-2"><button onClick={() => setStage('learning')} className="inline-flex items-center gap-2 rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-brand-700"><BookOpen className="h-4 w-4" />Back to lesson</button><button onClick={() => clearNuru()} className="inline-flex items-center gap-2 rounded-xl border border-ink-300 px-4 py-2.5 text-sm font-semibold text-ink-700 hover:bg-ink-50">Try another question</button></div></div> : <><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-50 text-brand-600"><Sparkles className="h-5 w-5" /></span><div><p className="text-xs font-bold uppercase tracking-wider text-brand-600">Learning workspace</p><h3 className="text-heading mt-1 text-xl font-bold">Nuru is ready when you are.</h3></div></div><p className="text-body mt-5">Open the Nuru assistant in the bottom corner and choose a contextual question. The answer will appear here inside the lesson workspace, not as a separate chat.</p><div className="mt-7 rounded-xl border border-ink-200 bg-ink-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-ink-500">Current context</p><div className="mt-4 grid gap-3 sm:grid-cols-3"><div><p className="text-xs text-ink-500">Subject</p><p className="mt-1 text-sm font-bold text-ink-800">Mathematics</p></div><div><p className="text-xs text-ink-500">Topic</p><p className="mt-1 text-sm font-bold text-ink-800">Algebra</p></div><div><p className="text-xs text-ink-500">Lesson</p><p className="mt-1 text-sm font-bold text-ink-800">Linear Equations</p></div></div></div></>}</div></Reveal><aside className="space-y-5"><div className="rounded-2xl border border-accent-200 bg-accent-50 p-5"><div className="flex items-start gap-3"><CheckCircle2 className="mt-0.5 h-5 w-5 shrink-0 text-accent-700" /><div><p className="text-sm font-bold text-accent-900">Demo note</p><p className="mt-1 text-xs leading-relaxed text-accent-800">Nuru appears throughout the learning experience and responds using the student's current learning context.</p></div></div></div><div className="surface p-5"><p className="text-xs font-bold uppercase tracking-wider text-ink-400">{nuruQuestion ? 'Selected question' : 'Suggested questions'}</p><p className="mt-2 text-sm font-semibold text-ink-800">{nuruQuestion ?? 'Choose from the Nuru assistant below.'}</p><button onClick={() => setStage('learning')} className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-brand-600 hover:text-brand-700"><ArrowLeft className="h-4 w-4" />Return to learning</button></div></aside></div><Reveal delay={100} className="mt-5"><div className="flex items-center gap-2 rounded-xl border border-ink-200 bg-white px-4 py-3 text-xs text-ink-500"><ArrowRight className="h-4 w-4 text-brand-600" />Nuru stays brief, supportive and focused on helping students think through the next step.</div></Reveal></div>;
}
