import { Check, ChevronRight, Circle, Code2, LockKeyhole, Palette, Sparkles } from 'lucide-react';
import { pathwayData, webCreatorModules } from '../data';
import { useDemo } from '../DemoContext';
import { Badge } from '@/components/ui/Badge';
import { ProgressBar } from '@/components/ui/ProgressBar';
import { Reveal } from '@/components/ui/Reveal';

const styles = { clean: 'bg-white text-slate-700 border-slate-200 font-sans', bold: 'bg-brand-700 text-white border-brand-800 font-display', editorial: 'bg-accent-50 text-ink-900 border-accent-300 font-display italic' };

export function SkillsLabView() {
  const { skillsStyle, setSkillsStyle, projectContinued, continueProject } = useDemo();
  return <div><Reveal><p className="text-eyebrow">Skills Lab</p><h2 className="text-heading mt-2 text-3xl font-bold sm:text-4xl">Explore practical skills by building real projects.</h2><p className="text-body mt-2 max-w-2xl">Project-oriented pathways help students move from curiosity to something they can show, share and improve.</p></Reveal><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{pathwayData.map((pathway, index) => <Reveal key={pathway.name} delay={index * 70}><div className={`surface h-full p-5 ${pathway.name === 'Web Creator' ? 'ring-2 ring-brand-500/20' : ''}`}><div className="flex items-start justify-between"><span className={`flex h-11 w-11 items-center justify-center rounded-xl ${pathway.name === 'Web Creator' ? 'bg-brand-50 text-brand-600' : 'bg-ink-100 text-ink-500'}`}><pathway.icon className="h-5 w-5" /></span>{pathway.name === 'Web Creator' ? <Badge variant="brand">Active</Badge> : <Badge variant="neutral">{pathway.status}</Badge>}</div><h3 className="font-display mt-5 font-bold text-ink-900">{pathway.name}</h3><p className="mt-2 text-sm text-ink-500">{pathway.description}</p><p className="mt-4 text-xs font-semibold text-ink-400">{pathway.modules} modules</p></div></Reveal>)}</div><div className="mt-7 grid gap-5 xl:grid-cols-[minmax(0,1fr)_20rem]"><Reveal><div className="surface p-5 sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><Badge variant="accent">Web Creator</Badge><h3 className="text-heading mt-3 text-2xl font-bold">Build and publish your first website.</h3><p className="mt-2 text-sm text-ink-500">Amani's pathway · 38% complete</p></div><ProgressBar value={38} className="w-32" /></div><div className="mt-7 grid gap-6 lg:grid-cols-[minmax(0,1fr)_15rem]"><div><p className="text-xs font-bold uppercase tracking-wider text-ink-400">Learning path</p><div className="mt-3 space-y-1">{webCreatorModules.map((module) => <div key={module.title} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${module.status === 'current' ? 'bg-brand-50 font-bold text-brand-700' : 'text-ink-600'}`}>{module.status === 'complete' ? <span className="flex h-5 w-5 items-center justify-center rounded-full bg-emerald-100 text-emerald-700"><Check className="h-3 w-3" /></span> : module.status === 'locked' ? <LockKeyhole className="h-4 w-4 text-ink-300" /> : <Circle className="h-4 w-4 fill-brand-500 text-brand-500" />}{module.title}{module.status === 'current' && <ChevronRight className="ml-auto h-4 w-4" />}</div>)}</div></div><div className="rounded-xl border border-ink-200 p-4"><div className="flex items-center gap-2"><Palette className="h-4 w-4 text-brand-600" /><p className="text-xs font-bold uppercase tracking-wider text-brand-700">Current activity</p></div><h4 className="mt-3 font-display font-bold text-ink-900">Make this heading stand out.</h4><div className={`mt-4 flex min-h-24 items-center justify-center rounded-lg border p-4 text-center text-xl ${styles[skillsStyle]}`}>Welcome to My Website</div><p className="mt-3 text-xs text-ink-500">Choose a style to update the real project preview.</p><div className="mt-3 grid grid-cols-3 gap-2">{(['clean', 'bold', 'editorial'] as const).map((style) => <button key={style} onClick={() => setSkillsStyle(style)} className={`rounded-lg border px-2 py-2 text-xs font-semibold capitalize ${skillsStyle === style ? 'border-brand-500 bg-brand-50 text-brand-700' : 'border-ink-200 text-ink-600 hover:bg-ink-50'}`}>{style}</button>)}</div></div></div><p className="mt-6 border-t border-ink-200 pt-5 text-sm text-ink-600"><strong className="text-ink-800">Students learn by changing real project elements</strong> rather than only reading lessons.</p></div></Reveal><Reveal delay={100}><div className="space-y-5"><div className="surface p-5"><div className="flex items-center gap-2"><Code2 className="h-4 w-4 text-brand-600" /><h3 className="font-display font-bold text-ink-900">Amani's Project</h3></div><p className="mt-4 font-display text-lg font-bold text-ink-900">My First Portfolio</p><Badge variant={projectContinued ? 'success' : 'warning'} className="mt-2">{projectContinued ? 'Milestone started' : 'In progress'}</Badge><div className="mt-4 space-y-2 text-xs text-ink-600"><p>Files: <span className="font-semibold">Home page · About section</span></p><p>Next milestone: <span className="font-semibold">Style your homepage</span></p></div><button onClick={continueProject} className="mt-5 w-full rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-brand-700">{projectContinued ? 'Project workspace open' : 'Continue Project'}</button></div><div className="rounded-2xl border border-brand-200 bg-brand-50 p-5"><div className="flex items-center gap-2"><Sparkles className="h-4 w-4 text-brand-600" /><p className="text-sm font-bold text-brand-900">Why project-based?</p></div><p className="mt-2 text-xs leading-relaxed text-brand-800">Every pathway connects a concept to something students can create, test and make their own.</p></div></div></Reveal></div></div>;
}
