import {useState} from 'react';
import {Lightbulb,Sparkles,X} from 'lucide-react';
import {useNavigate,useLocation} from 'react-router-dom';
import {algebraSuggestion,cssSuggestion} from '@/services/nuru/MockContentAIProvider';
const demoSuggestions=[algebraSuggestion,cssSuggestion];
const reasons=['Not relevant','Already covered','Wrong level',"We'll create manually",'Other'];
export function ContentOpportunitiesPage(){const navigate=useNavigate(),{pathname}=useLocation(),global=pathname.startsWith('/platform');const [dismiss,setDismiss]=useState<string|null>(null),[gone,setGone]=useState<string[]>([]);return <div><p className="text-xs font-bold uppercase tracking-[.18em] text-violet-700">Nuru opportunities</p><h1 className="mt-2 font-display text-3xl font-bold">Content opportunity dashboard</h1><p className="mt-2 text-sm text-slate-600">Structure-based signals only. Each recommendation includes evidence and requires human authorization before drafting.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{demoSuggestions.filter(s=>!gone.includes(s.id)).map(s=><article key={s.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between"><Lightbulb className="text-violet-600"/><span className={`rounded-full px-2 py-1 text-xs font-bold uppercase ${s.priority==='high'?'bg-red-50 text-red-700':'bg-amber-50 text-amber-700'}`}>{s.priority}</span></div><p className="mt-4 text-xs font-bold uppercase text-slate-400">{s.type.replace(/_/g,' ')}</p><h2 className="mt-1 text-lg font-bold">{s.title.replace('Add ','')}</h2><div className="mt-3 rounded-xl bg-slate-50 p-3 text-sm"><strong>Suggested because…</strong><p className="mt-1 text-slate-600">{s.reason}</p></div><div className="mt-4 flex gap-2"><button onClick={()=>navigate(global?'/platform/content/studio':'/school/content/studio')} className="rounded-lg bg-violet-700 px-3 py-2 text-sm font-bold text-white"><Sparkles className="mr-1 inline" size={15}/>Review</button><button onClick={()=>setDismiss(s.id)} className="rounded-lg border px-3 py-2 text-sm font-bold"><X className="mr-1 inline" size={15}/>Dismiss</button></div>{dismiss===s.id&&<div className="mt-4 border-t pt-3"><p className="text-xs font-bold">Why are you dismissing this?</p><div className="mt-2 flex flex-wrap gap-2">{reasons.map(r=><button onClick={()=>setGone([...gone,s.id])} key={r} className="rounded-full border bg-white px-2.5 py-1 text-xs">{r}</button>)}</div></div>}</article>)}</div><div className="mt-6 rounded-xl border border-blue-100 bg-blue-50 p-4 text-sm text-blue-900"><strong>Privacy boundary:</strong> {global?'Global opportunities use platform structure only and do not expose private school learning data.':'These opportunities are scoped to Greenfield Academy and are not shared with other schools.'}</div></div>}
